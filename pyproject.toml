[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "sectool"
version = "1.2.0"
description = "Cybersecurity multi-tool CLI: code, SSL/TLS, dependency, packet, password, crypto, HTTP-header, HTTP-probe, HTTP-method and web-fuzzing auditing"
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "sectool contributors" }]
keywords = ["security", "cli", "sast", "ssl", "tls", "cve", "osv", "passwords", "crypto", "fuzzing", "http-headers", "sarif"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Intended Audience :: Information Technology",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Topic :: Security",
]
dependencies = [
    "requests>=2.31,<3",
    "colorama>=0.4.6,<0.5",
    "cryptography>=42",
]

[project.optional-dependencies]
packets = ["scapy>=2.5,<3"]
dev = ["pytest>=8"]

[project.urls]
Homepage = "https://github.com/NormalLinuxUser2/sectool"
Source = "https://github.com/NormalLinuxUser2/sectool"
Issues = "https://github.com/NormalLinuxUser2/sectool/issues"
Changelog = "https://github.com/NormalLinuxUser2/sectool/blob/main/CHANGELOG.md"

[project.scripts]
sectool = "sectool.cli:main"

[tool.setuptools.packages.find]
include = ["sectool*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
